-- Удаляем старую БД, если есть
DROP DATABASE IF EXISTS logistics_db;
CREATE DATABASE logistics_db CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE logistics_db;

-- Таблица пользователей
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    login VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(100) NOT NULL,
    full_name VARCHAR(100) NOT NULL,
    role ENUM('admin','manager','driver') NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- Таблица заказов
CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    description TEXT NOT NULL,
    status VARCHAR(20) DEFAULT 'created',
    driver_id INT NULL,
    created_by INT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (driver_id) REFERENCES users(id) ON DELETE SET NULL,
    FOREIGN KEY (created_by) REFERENCES users(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

-- Тестовые данные

-- Администратор (логин: admin, пароль: admin)
INSERT INTO users (login, password, full_name, role) VALUES 
('admin', 'admin', 'Главный Администратор', 'admin');

-- Менеджеры
INSERT INTO users (login, password, full_name, role) VALUES 
('manager1', 'pass123', 'Иван Петров', 'manager'),
('manager2', 'pass123', 'Ольга Смирнова', 'manager');

-- Водители
INSERT INTO users (login, password, full_name, role) VALUES 
('driver1', 'pass123', 'Сергей Иванов', 'driver'),
('driver2', 'pass123', 'Анна Кузнецова', 'driver'),
('driver3', 'pass123', 'Дмитрий Соколов', 'driver');

-- Заказы (созданы менеджерами)
INSERT INTO orders (description, status, created_by) VALUES 
('Перевозка груза из Москвы в Питер', 'created', 2),
('Доставка мебели в офис', 'assigned', 3),
('Срочная доставка документов', 'in_progress', 2),
('Переезд склада', 'created', 3),
('Заказ для клиента А', 'completed', 2);

-- Назначим водителей на некоторые заказы
UPDATE orders SET driver_id = 4 WHERE id = 2;  -- driver1
UPDATE orders SET driver_id = 5, status = 'in_progress' WHERE id = 3; -- driver2
UPDATE orders SET driver_id = 6 WHERE id = 5; -- driver3 (но статус completed)